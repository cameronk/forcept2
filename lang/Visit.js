/**
 *
 */

import { defineMessages } from 'react-intl';
const root = "visit.";

export default defineMessages({

    ///
    createNew: {
        id: root + "createNew",
        defaultMessage: "Create a new visit"
    }

});
